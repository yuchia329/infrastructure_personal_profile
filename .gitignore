# Secret variable values — never commit!
terraform.tfvars

# Terraform state and lock files — commit .tfstate only if NOT using remote backend
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.backup
*.backup
crash.log
*.pem